services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: uca
    ports:
      - "5432:5432"
    volumes:
      - uca-pgdata:/var/lib/postgresql/data

volumes:
  uca-pgdata:
