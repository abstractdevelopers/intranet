import { AdminSection } from "@/components/ui/placeholder";

export const metadata = { title: "Analytics" };

export default function AdminAnalyticsPage() {
  return (
    <AdminSection
      title="Analytics"
      description="Completion rates, course popularity, trial conversion and revenue analytics are part of the next build phase."
    />
  );
}
