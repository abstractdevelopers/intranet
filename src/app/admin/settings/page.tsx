import { AdminSection } from "@/components/ui/placeholder";

export const metadata = { title: "Settings" };

export default function AdminSettingsPage() {
  return (
    <AdminSection
      title="Settings"
      description="Academy configuration, roles and billing settings are part of the next build phase."
    />
  );
}
