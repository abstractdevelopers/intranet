import { AdminSection } from "@/components/ui/placeholder";

export const metadata = { title: "Calendar" };

export default function AdminCalendarPage() {
  return (
    <AdminSection
      title="Calendar"
      description="Academy events, workshops and scheduling tools are part of the next build phase."
    />
  );
}
