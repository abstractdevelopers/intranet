import { AdminSection } from "@/components/ui/placeholder";

export const metadata = { title: "Assignments" };

export default function AdminAssignmentsPage() {
  return (
    <AdminSection
      title="Assignments"
      description="Assignment creation, deadlines and submission-type configuration are part of the next build phase."
    />
  );
}
