import { AdminSection } from "@/components/ui/placeholder";

export const metadata = { title: "Announcements" };

export default function AdminAnnouncementsPage() {
  return (
    <AdminSection
      title="Announcements"
      description="Publishing academy, course and cohort announcements is part of the next build phase."
    />
  );
}
